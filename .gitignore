# ── Internal / confidential ──────────────────────────────────────────
# Source material, specs, and business docs live in the PRIVATE repo:
#   https://github.com/achisolomon/hearfy-internal
# This repo is public: demo application code only.

docs/
internal/

# Belt-and-braces: block these by type/pattern anywhere in the tree,
# so a stray copy outside docs/ still can't be committed.
*.docx
*.pptx
*.pdf
*SOURCES*
*persona*
# …but the demo's own persona code is application source, not a spec: it holds
# names, initials and avatar colours for four fictional characters. Exempted by
# exact path, so a stray `persona-spec.md` anywhere still gets blocked.
!demo/lib/personas.ts
!demo/components/persona-avatar.tsx
*pitch*deck*
*Pitch*Deck*
*MRD*
*Product?Bible*

# ── Build artefacts ──────────────────────────────────────────────────
# The demo app has its own .gitignore for node_modules/.next/out; this
# catches a stray install at the repo root, which nothing else covers.
node_modules/

# ── OS / editor ──────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
*.swp
*~
.idea/
.vscode/

# ── Worktrees ────────────────────────────────────────────────────────
# Isolated workspaces created per-feature live here; never committed.
.claude/worktrees/
