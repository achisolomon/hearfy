"use client";
import { Bell,CalendarDays,ChevronRight,HeartPulse,Home,ShieldCheck,Star } from "lucide-react";
import { Card,BrandLogo,PageHeader,PrimaryButton,SecondaryButton,SecureFooter,StatusPill } from "../../ui";
import { BRAND_NAME, appointment, cma, patient } from "@/lib/mock-data";
import { ScreenId } from "../registry";
import { Shell, Avatar } from "../shared";

/**
 * The first screen anyone sees, so its faults were the loudest (walked at
 * 390px, 2026-08-31):
 *
 * - `min-h-[730px]` was a fixed pixel height on a column that already sits
 *   inside Shell's own padding, so on a 844px phone it pushed "Get started"
 *   under the docked demo bar and the screen's one primary action was cut in
 *   half. It was also the one number here that could not scale with the
 *   text-size control. The column now asks for no minimum height at all:
 *   Shell's own pb-40 is what clears the docked bars, and the hero plus the
 *   two actions are shorter than a phone viewport regardless. (A viewport
 *   unit is not the fix either — `min-h-[100svh]` on a column *inside* that
 *   padding just pushes the actions the padding's worth below the fold.)
 * - `mt-28` reserved 7rem of empty space above the headline for a decorative
 *   ring that is mostly outside the card, leaving a large blank band at the
 *   top of the hero. The ring is now inset so it reads as intentional, and
 *   the copy sits on normal rhythm.
 * - "starts here." was Vital Teal on a near-white gradient: 2.68:1, under the
 *   3:1 that even display text needs. Teal Ink keeps the two-tone headline
 *   while actually being readable.
 * - "Explore the demo" was a bare text button with no bounds and a 23px hit
 *   area; it is now the SecondaryButton this system already defines.
 */
export function Welcome({go}:{go:(s:ScreenId)=>void}){return <Shell><div className="flex flex-col"><BrandLogo/><div className="relative mt-9 overflow-hidden rounded-[34px] bg-gradient-to-br from-[#dff7f5] via-white to-[#eff6fa] p-7"><div className="pointer-events-none absolute -right-6 -top-6 h-32 w-32 rounded-full border-[20px] border-white/50" aria-hidden/><div className="relative mt-10"><span className="text-xs font-extrabold uppercase tracking-[.18em] text-teal-ink">Hearing care, at home.</span><h1 className="mt-3 text-balance text-[42px] font-extrabold leading-[.98] tracking-[-.04em]">Better hearing<br/><span className="text-teal-ink">starts here.</span></h1><p className="mt-5 max-w-[280px] leading-6 text-slate-500">A complete hearing test in the comfort and privacy of your home.</p></div><div className="mt-8 flex items-center gap-3 rounded-2xl bg-white/90 p-4 shadow-soft"><div className="grid h-12 w-12 shrink-0 place-items-center rounded-2xl bg-[#e7f8f7] text-teal-ink"><Home/></div>{/* min-w-0: a flex child will not shrink below its content width without it, so at 360px with both text scales at maximum this pair pushed out of the card. */}<div className="min-w-0"><b>Professional home visit</b><p className="text-xs text-slate-500">CMA on-site. Audiologist online.</p></div></div></div><div className="mt-8"><PrimaryButton onClick={()=>go("signin")}>Get started</PrimaryButton><div className="mt-3"><SecondaryButton onClick={()=>go("home")}>Explore the demo</SecondaryButton></div></div></div></Shell>}
export function SignIn({go,back}:{go:(s:ScreenId)=>void;back:()=>void}){return <Shell><PageHeader onBack={back} title={`Welcome to ${BRAND_NAME.toUpperCase()}`} subtitle="Enter your mobile number. We’ll send you a secure verification code." eyebrow="Secure access"/><label htmlFor="mobile" className="block text-sm font-bold">Mobile number</label><div className="mt-2 flex h-16 items-center rounded-2xl border border-[#dce8ea] bg-white px-4 focus-within:border-brand-teal focus-within:ring-2 focus-within:ring-brand-teal/25"><span className="border-r border-[#e0e9ea] pr-3 font-bold">+1</span><input id="mobile" type="tel" autoComplete="tel-national" inputMode="numeric" className="h-full w-full bg-transparent px-3 outline-none placeholder:text-slate-500" placeholder="(305) 555-0148"/></div><Card className="mt-5 flex gap-3 p-4"><ShieldCheck className="text-teal-ink"/><p className="text-sm leading-6 text-slate-500">Your information is encrypted and only shared with your care team.</p></Card><div className="mt-7"><PrimaryButton onClick={()=>go("home")}>Send code</PrimaryButton></div></Shell>}
/**
 * Home has two states. Before booking it is the prospect's starting point —
 * stage 1 is Awareness, and the visit is not confirmed until stage 2 — so
 * showing a scheduled visit there claimed a booking the viewer had not made
 * yet, with a date they had never chosen.
 */
export function HomeScreen({go,booked=false}:{go:(s:ScreenId)=>void;booked?:boolean}){return <Shell><div className="mb-5 flex items-center justify-between"><BrandLogo/><button className="grid h-11 w-11 place-items-center rounded-full bg-white shadow-soft"><Bell size={19}/></button></div><p className="text-sm text-slate-500">Hello, {patient.name} 👋</p><h1 className="mt-1 text-[30px] font-extrabold tracking-[-.03em]">Your care, simplified.</h1>{booked?<Card className="mt-5 overflow-hidden p-5"><div className="flex items-center justify-between"><StatusPill>Visit scheduled</StatusPill><span className="text-xs font-semibold text-slate-500">Tomorrow</span></div><h2 className="mt-5 text-[26px] font-extrabold leading-tight">Your hearing visit<br/>is ready.</h2><p className="mt-2 text-sm text-slate-500">{appointment.date} · {appointment.time}</p><div className="mt-5 flex items-center gap-3"><Avatar/><div className="flex-1"><b>{cma.name}</b><p className="text-xs text-slate-500">Certified Medical Assistant</p><p className="mt-1 flex items-center gap-1 text-xs"><Star size={13} fill="currentColor"/> {cma.rating} ({cma.reviews})</p></div></div><button onClick={()=>go("assigned")} className="mt-5 flex h-14 w-full items-center justify-between rounded-2xl bg-brand-navy px-4 py-3.5 font-bold text-white shadow-soft transition hover:bg-[#102d51]">View visit details<ChevronRight size={18}/></button></Card>:<Card className="mt-5 overflow-hidden p-5"><StatusPill>No visit booked yet</StatusPill><h2 className="mt-5 text-[26px] font-extrabold leading-tight">A hearing test,<br/>at your kitchen table.</h2><p className="mt-2 text-sm text-slate-500">A clinician comes to you. About an hour, ${appointment.price}.</p><button onClick={()=>go("intake-for")} className="mt-5 flex min-h-14 w-full items-center justify-between rounded-2xl bg-brand-navy px-4 py-3.5 font-bold text-white shadow-soft transition hover:bg-[#102d51]">Book a visit<ChevronRight size={18}/></button></Card>}{booked&&<><h3 className="mb-3 mt-6 font-extrabold">What’s next?</h3><div className="grid grid-cols-2 gap-3"><button onClick={()=>go("intake-for")} className="rounded-[22px] bg-white p-4 text-left shadow-card"><HeartPulse className="text-teal-ink"/><b className="mt-5 block">Complete intake</b><span className="mt-1 block text-xs text-slate-500">5 simple steps</span></button><button onClick={()=>go("book-date")} className="rounded-[22px] bg-white p-4 text-left shadow-card"><CalendarDays className="text-teal-ink"/><b className="mt-5 block">Manage visit</b><span className="mt-1 block text-xs text-slate-500">Date and time</span></button></div><Card className="mt-4 p-4"><div className="flex items-center justify-between"><div><span className="text-xs font-bold uppercase tracking-wider text-teal-ink">Your journey</span><h3 className="mt-1 font-extrabold">Visit booked · intake still to do</h3></div><div className="grid h-12 w-12 place-items-center rounded-full bg-[#e9f8f7] font-extrabold text-teal-ink">50%</div></div><div className="mt-4 h-2 rounded-full bg-[#e8eeee]"><div className="h-full w-1/2 rounded-full bg-brand-teal"/></div></Card></>}<SecureFooter/></Shell>}
