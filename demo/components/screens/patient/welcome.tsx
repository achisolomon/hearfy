"use client";
import { Bell,CalendarDays,ChevronRight,HeartPulse,Home,ShieldCheck,Star } from "lucide-react";
import { Card,BrandLogo,PageHeader,PrimaryButton,SecureFooter,StatusPill } from "../../ui";
import { BRAND_NAME, appointment, cma, patient } from "@/lib/mock-data";
import { ScreenId } from "../registry";
import { Shell, Avatar } from "../shared";

export function Welcome({go}:{go:(s:ScreenId)=>void}){return <Shell><div className="flex min-h-[730px] flex-col"><BrandLogo/><div className="relative mt-9 overflow-hidden rounded-[34px] bg-gradient-to-br from-[#dff7f5] via-white to-[#eff6fa] p-7"><div className="absolute right-[-35px] top-[-25px] h-40 w-40 rounded-full border-[24px] border-white/50"/><div className="relative mt-28"><span className="text-xs font-extrabold uppercase tracking-[.18em] text-brand-teal">Hearing care, at home.</span><h1 className="mt-3 text-[42px] font-extrabold leading-[.98] tracking-[-.05em]">Better hearing<br/><span className="text-brand-teal">starts here.</span></h1><p className="mt-5 max-w-[280px] leading-6 text-slate-500">A complete hearing test in the comfort and privacy of your home.</p></div><div className="mt-8 flex items-center gap-3 rounded-2xl bg-white/90 p-4 shadow-soft"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-[#e7f8f7] text-brand-teal"><Home/></div><div><b>Professional home visit</b><p className="text-xs text-slate-500">CMA on-site. Audiologist online.</p></div></div></div><div className="mt-auto pt-7"><PrimaryButton onClick={()=>go("signin")}>Get started</PrimaryButton><button onClick={()=>go("home")} className="mt-4 w-full text-sm font-bold text-brand-navy">Explore the demo</button></div></div></Shell>}
export function SignIn({go,back}:{go:(s:ScreenId)=>void;back:()=>void}){return <Shell><PageHeader onBack={back} title={`Welcome to ${BRAND_NAME.toUpperCase()}`} subtitle="Enter your mobile number. We’ll send you a secure verification code." eyebrow="Secure access"/><label className="block text-sm font-bold">Mobile number</label><div className="mt-2 flex h-16 items-center rounded-2xl border border-[#dce8ea] bg-white px-4"><span className="border-r border-[#e0e9ea] pr-3 font-bold">+1</span><input className="w-full px-3 outline-none" placeholder="(305) 555-0148"/></div><Card className="mt-5 flex gap-3 p-4"><ShieldCheck className="text-brand-teal"/><p className="text-sm leading-6 text-slate-500">Your information is encrypted and only shared with your care team.</p></Card><div className="mt-7"><PrimaryButton onClick={()=>go("home")}>Send code</PrimaryButton></div></Shell>}
/**
 * Home has two states. Before booking it is the prospect's starting point —
 * stage 1 is Awareness, and the visit is not confirmed until stage 2 — so
 * showing a scheduled visit there claimed a booking the viewer had not made
 * yet, with a date they had never chosen.
 */
export function HomeScreen({go,booked=false}:{go:(s:ScreenId)=>void;booked?:boolean}){return <Shell><div className="mb-5 flex items-center justify-between"><BrandLogo/><button className="grid h-11 w-11 place-items-center rounded-full bg-white shadow-soft"><Bell size={19}/></button></div><p className="text-sm text-slate-500">Hello, {patient.name} 👋</p><h1 className="mt-1 text-[30px] font-extrabold tracking-[-.03em]">Your care, simplified.</h1>{booked?<Card className="mt-5 overflow-hidden border-0 bg-brand-navy p-5 text-white"><div className="flex items-center justify-between"><StatusPill>Visit scheduled</StatusPill><span className="text-xs text-white/65">Tomorrow</span></div><h2 className="mt-5 text-[26px] font-extrabold leading-tight">Your hearing visit<br/>is ready.</h2><p className="mt-2 text-sm text-white/70">{appointment.date} · {appointment.time}</p><div className="mt-5 flex items-center gap-3"><Avatar/><div className="flex-1"><b>{cma.name}</b><p className="text-xs text-white/65">Certified Medical Assistant</p><p className="mt-1 flex items-center gap-1 text-xs"><Star size={13} fill="currentColor"/> {cma.rating} ({cma.reviews})</p></div></div><button onClick={()=>go("assigned")} className="mt-5 flex h-13 w-full items-center justify-between rounded-2xl bg-white px-4 py-3.5 font-bold text-brand-navy">View visit details<ChevronRight size={18}/></button></Card>:<Card className="mt-5 overflow-hidden border-0 bg-brand-navy p-5 text-white"><StatusPill>No visit booked yet</StatusPill><h2 className="mt-5 text-[26px] font-extrabold leading-tight">A hearing test,<br/>at your kitchen table.</h2><p className="mt-2 text-sm text-white/70">A clinician comes to you. About an hour, ${appointment.price}.</p><button onClick={()=>go("intake-for")} className="mt-5 flex h-13 w-full items-center justify-between rounded-2xl bg-brand-teal px-4 py-3.5 font-bold text-white">Book a visit<ChevronRight size={18}/></button></Card>}{booked&&<><h3 className="mb-3 mt-6 font-extrabold">What’s next?</h3><div className="grid grid-cols-2 gap-3"><button onClick={()=>go("intake-for")} className="rounded-[22px] bg-white p-4 text-left shadow-card"><HeartPulse className="text-brand-teal"/><b className="mt-5 block">Complete intake</b><span className="mt-1 block text-xs text-slate-500">5 simple steps</span></button><button onClick={()=>go("book-date")} className="rounded-[22px] bg-white p-4 text-left shadow-card"><CalendarDays className="text-brand-teal"/><b className="mt-5 block">Manage visit</b><span className="mt-1 block text-xs text-slate-500">Date and time</span></button></div><Card className="mt-4 p-4"><div className="flex items-center justify-between"><div><span className="text-xs font-bold uppercase tracking-wider text-brand-teal">Your journey</span><h3 className="mt-1 font-extrabold">Visit booked · intake still to do</h3></div><div className="grid h-12 w-12 place-items-center rounded-full bg-[#e9f8f7] font-extrabold text-brand-teal">50%</div></div><div className="mt-4 h-2 rounded-full bg-[#e8eeee]"><div className="h-full w-1/2 rounded-full bg-brand-teal"/></div></Card></>}<SecureFooter/></Shell>}
